import { app, apiBaseUrl, port } from './server.js';
app.listen(port, () => {
    console.log(`OctoFit Tracker API is running on port ${port}`);
    console.log(`API base URL: ${apiBaseUrl}`);
});
//# sourceMappingURL=index.js.map